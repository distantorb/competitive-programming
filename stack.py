"""
Stack is an object that keeps track of a set of elements and provides
the following operations: test if the stack is empty, add an element 
to the top of the stack (push), access the element at the top of the
stack, and remove an element (pop).
"""
stack = []

stack.append("p")
stack.append("u")
stack.append("s")
stack.append("h")

print(stack)
